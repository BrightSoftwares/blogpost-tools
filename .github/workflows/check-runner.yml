name: Build and publish the check runner image

# Heavy Docker image build — NON-BLOCKING, decoupled from merge gating.
# Manual + weekly only (was firing-and-failing on every push via a stuck
# self-hosted runner).
on:
  workflow_dispatch:
  schedule:
    - cron: '15 5 * * 1'   # Mondays 05:15 UTC — periodic image refresh

jobs:
  docker:
    uses: BrightSoftwares/blogpost-tools/.github/workflows/build_and_publish_docker.yml@main
    with:
      image_name: check-runner
      tag: latest
      subfolder_name: check-runner
      dockerfile_name: Dockerfile.check-runner
    secrets:
      registry_username: ${{ secrets.DOCKER_USERNAME }}
      registry_password: ${{ secrets.DOCKER_PASSWORD }}
